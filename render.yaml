services:
  - type: web
    name: geoattend-realtime
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: OTP_MODE
        value: demo
